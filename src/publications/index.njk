---
layout: layout
title: Publications
description: Selected papers and reports authored by Christopher Fiorelli.
permalink: publications/index.html
---
{% set sorted = publications | sort(attribute='year', reverse=true) %}

<section class="publications-hero" aria-labelledby="publications-title">
  <div class="publications-hero__inner">
    <div class="publications-hero__copy">
      <p class="publications-hero__eyebrow">Research highlights</p>
      <h1 id="publications-title">Publications</h1>
      <p class="publications-hero__summary">A snapshot of the papers and reports I've contributed to—spanning open research platforms, large language model safety, and reproducibility practices in AI.</p>
    </div>
    <div class="publications-hero__sparkle" aria-hidden="true">
      <span>✶</span>
      <span>✸</span>
      <span>✺</span>
    </div>
  </div>
</section>

{% set currentYear = null %}
{% for item in sorted %}
  {% if item.year != currentYear %}
    {% if currentYear is not none %}
        </ol>
      </section>
    {% endif %}
    {% set currentYear = item.year %}
  <section class="publications-year" aria-labelledby="year-{{ currentYear }}">
    <h2 id="year-{{ currentYear }}">{{ currentYear }}</h2>
    <ol class="publications-list">
  {% endif %}
      <li class="publications-entry">
        <article>
          <p class="citation">
            <span class="year">({{ item.year }})</span>.
            {% if item.url %}
              <a href="{{ item.url }}" target="_blank" rel="noopener noreferrer">{{ item.title }}</a>.
            {% else %}
              {{ item.title }}.
            {% endif %}
            <em class="venue">{{ item.venue }}</em>.
          </p>
        </article>
      </li>
{% endfor %}
{% if currentYear is not none %}
    </ol>
  </section>
{% endif %}
