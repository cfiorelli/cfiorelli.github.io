---
layout: layout
title: Home
description: C. Fiorelli — Technical Product & Program Manager building production AI systems, computer vision products, and data platforms.
permalink: index.html
---
{% set sortedProjects = projects | sort(attribute='updated') | reverse %}
{% set curiositiesList = curiosities or [] %}

<section class="hero">
  <h1 class="hero-title">Building AI Systems at the Intersection of Safety and Innovation</h1>
  <p class="hero-sub">Technical Product & Program Manager with 10+ years leading AI/ML initiatives across research, production systems, and safety-critical applications.</p>
  <p class="hero-meta">Currently: Computer vision for aviation safety. Previously: Ai2, Google Geo, Microsoft Bing ML. Seeking senior TPM/PM roles in AI product development.</p>
</section>

<section class="projects-preview" aria-labelledby="projects-heading">
  <div class="section-heading">
    <h2 id="projects-heading">Featured projects</h2>
    <a class="section-link" href="/projects/">View all projects →</a>
  </div>

  <div class="grid project-grid">
    {% for project in sortedProjects %}
      {% if loop.index <= 3 %}
        {% set links = project.links or {} %}
        {% set caseStudyUrl = links.caseStudy or ('/projects/' ~ project.slug ~ '/') %}
        {% set cardImage = project.image or project.heroImage %}
        {% set cardImage2x = project.image2x or project.heroImage %}
        {% set cardAlt = project.imageAlt or project.title %}
        <article class="card project-card">
          {% if cardImage %}
            <div class="card-media">
              <img src="{{ cardImage }}"
                   {% if cardImage2x %}srcset="{{ cardImage }} 1x, {{ cardImage2x }} 2x"{% endif %}
                   sizes="(min-width: 960px) 320px, (min-width: 640px) 45vw, 90vw"
                   loading="lazy"
                   decoding="async"
                   alt="{{ cardAlt }}">
            </div>
          {% endif %}
          <div class="card-body">
            {% if project.status %}
              {% set statusPercent = project.status.percent | default(0) %}
              {% set statusLabel = project.status.label %}
              <div class="project-status"{% if project.status.note %} title="{{ project.status.note }}"{% endif %} aria-label="{{ statusPercent }}% complete{% if statusLabel %} — {{ statusLabel }}{% endif %}">
                <span class="project-status__percent">{{ statusPercent }}%</span>
                {% if statusLabel %}
                  <span class="project-status__label">{{ statusLabel }}</span>
                {% endif %}
              </div>
            {% endif %}
            <h3>{{ project.title }}</h3>
            {% if project.tagline %}
              <p class="muted">{{ project.tagline }}</p>
            {% endif %}
            {% if project.summary %}
              <p>{{ project.summary }}</p>
            {% endif %}
            {% if project.tags %}
              <div class="tags">
                {% for tag in project.tags %}
                  <span>{{ tag }}</span>
                {% endfor %}
              </div>
            {% endif %}
            <div class="card-actions">
              <a class="btn" href="{{ caseStudyUrl }}">Project page</a>
              {% if links.repo %}
                <a class="btn ghost" href="{{ links.repo }}" target="_blank" rel="noopener noreferrer">Source</a>
              {% endif %}
            </div>
          </div>
        </article>
      {% endif %}
    {% endfor %}
  </div>
</section>

<section class="interests-preview" aria-labelledby="interests-heading">
  <div class="section-heading">
    <h2 id="interests-heading">Latest interests</h2>
    <a class="section-link" href="/interests/">Browse all interests →</a>
  </div>
  <ul class="interests-list">
    {% for item in curiositiesList %}
      {% if loop.index <= 5 %}
        {% set displayDate = item.addedAt or item.added %}
        <li>
          {% if item.url %}
            <a href="{{ item.url }}" target="_blank" rel="noopener noreferrer">{{ item.title }}</a>
          {% else %}
            {{ item.title }}
          {% endif %}
          <div class="interest-meta">
            {% if displayDate %}
              <span class="muted">{{ displayDate }}</span>
            {% endif %}
            {% if item.sourceLabel %}
              <span class="badge">{{ item.sourceLabel }}</span>
            {% endif %}
            {% if item.category %}
              <span class="muted">{{ item.category }}</span>
            {% endif %}
          </div>
          {% if item.tags %}
            <span class="tags-inline">
              {% for tag in item.tags %}
                <span>{{ tag }}</span>
              {% endfor %}
            </span>
          {% endif %}
        </li>
      {% endif %}
    {% endfor %}
  </ul>
</section>

<section class="about-preview" aria-labelledby="about-heading">
  <div class="about-preview-card">
    <h2 id="about-heading">About Christopher</h2>
    <p class="muted">Technical Product & Program Manager with 10+ years leading AI/ML programs across research, product development, and operations. Recent completion of AI Product Management program at University of Washington. Expertise in computer vision, data platforms, regulatory compliance, and cross-functional leadership. Hands-on with Python, SQL, and production ML systems.</p>
    <ul class="about-preview-list">
      <li>Currently building computer vision systems for aviation safety at a stealth startup (launching 2026)</li>
      <li>Professional experience: <a href="https://allenai.org" target="_blank" rel="noopener noreferrer">Ai2</a>, <a href="https://mapsplatform.google.com/resources/blog/?tags=&year=all" target="_blank" rel="noopener noreferrer">Google Geo (Street View / Maps)</a>, <a href="https://www.microsoft.com/en-us/research/" target="_blank" rel="noopener noreferrer">Microsoft Bing ML</a></li>
      <li>Coordinated <a href="https://defcon.org/" target="_blank" rel="noopener noreferrer">Defcon</a> <a href="https://aivillage.org/" target="_blank" rel="noopener noreferrer">AI Village</a> 2024 red-teaming event for Ai2's OLMo LLM</li>
      <li>Seeking senior TPM/PM roles in AI product development and safety-critical systems</li>
    </ul>
    <div class="card-actions">
      <a class="btn subtle" href="/about/">Read the full bio →</a>
    </div>
  </div>
</section>
